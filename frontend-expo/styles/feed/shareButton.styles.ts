import { StyleSheet } from "react-native";

export const shareButtonStyles = StyleSheet.create({
  container: {
    alignItems: "center",
    marginBottom: 20,
  },

  text: {
    color: "#FFFFFF",
    marginTop: 4,
    fontSize: 12,
    fontWeight: "600",
  },
});