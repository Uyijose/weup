import { StyleSheet } from "react-native";

export const likeButtonStyles = StyleSheet.create({
  container: {
    alignItems: "center",
    marginBottom: 20,
  },

  text: {
    color: "#FFFFFF",
    fontSize: 12,
    fontWeight: "600",
    marginTop: 4,
  },
});