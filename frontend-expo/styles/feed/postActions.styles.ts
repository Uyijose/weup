import { StyleSheet } from "react-native";

export const postActionsStyles = StyleSheet.create({
  container: {
    position: "absolute",
    right: 14,
    bottom: 130,
    alignItems: "center",
  },

  button: {
    alignItems: "center",
    marginBottom: 20,
  },

  count: {
    color: "#FFFFFF",
    fontSize: 12,
    fontWeight: "600",
    marginTop: 4,
  },
});