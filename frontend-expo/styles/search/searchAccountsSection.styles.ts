import { StyleSheet } from "react-native";

export const searchAccountsSectionStyles = StyleSheet.create({
  container: {
    paddingTop: 8,
  },

  title: {
    paddingHorizontal: 16,
    marginBottom: 4,
    fontSize: 18,
    fontWeight: "700",
    color: "#FFFFFF",
  },

  emptyText: {
    paddingHorizontal: 16,
    paddingVertical: 14,
    fontSize: 14,
    color: "#999999",
  },
});